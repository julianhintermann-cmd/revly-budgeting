[project]
name = "revly-budgeting-backend"
version = "0.1.0"
description = "Backend of revly-budgeting: self-hosted envelope budgeting"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
filterwarnings = [
    "ignore::DeprecationWarning",
]
